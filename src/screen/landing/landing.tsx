import React from 'react';
import {Box} from 'native-base';
import KeyboardInteraction from '../../components/keyboard-interaction/keyboard-interaction';

function Landing() {
  return (
    <Box py="40px" px="18px">
      <KeyboardInteraction />
    </Box>
  );
}

export default Landing;
